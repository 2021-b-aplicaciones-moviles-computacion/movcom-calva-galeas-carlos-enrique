<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.examen2b_receta_ingredientes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Examen2B_Receta_Ingredientes">

        <activity android:name=".IngredientesFormularioActualizacion" />
        <activity android:name=".IngredientresFormularioRegistro" />
        <activity android:name=".RecetasFormularioActualizacion" />
        <activity android:name=".RecetasFormularioRegistro" />
        <activity android:name=".IngredientesActivity" />
        <activity android:name=".RecetasActivity" />
        <activity android:name=".MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    </application>

</manifest>